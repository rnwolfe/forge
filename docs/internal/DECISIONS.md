# Architectural Decision Records

Record significant decisions here as the project evolves.

## ADR-001 -- forge.toml as single config source

**Date**: Generated by /onboard
**Status**: Accepted

**Context**: The autodev pipeline needs configuration for repo, stack, gating, trust, and
protected files. Options: environment variables, YAML, TOML, JSON.

**Decision**: Use a single `forge.toml` file at the repo root. Shell scripts parse it with
a lightweight awk function (no dependencies). TOML is human-readable, well-commented, and
familiar to most developers.

**Consequences**: All pipeline scripts must source `config.sh` which reads `forge.toml`.
Adding new config keys requires updating both `forge.toml` (with defaults) and `config.sh`
(with the awk parser call).
